import: [lookup_embedder, complex]

factor:
  feature_file: None
  # use indicator features for the existence of attributes instead of attribute values
  features_as_indicator: False
  class_name: FactorGraphModel
  base_model:
    type: complex