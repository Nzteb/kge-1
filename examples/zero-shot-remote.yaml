dataset.name: zero-shot-head-10
job.type: zero_shot
zero_shot:
  #{fold_in, closed_form}
  type: fold_in
  fold_in.max_triple: -1
  fold_in.incremental: True
  fold_in.freeze: True
  obtain_seen_model: load
  # {all, incremental}
  eval_type: incremental
  # remote
  seen_checkpoint: /work/pbetz/kge_local/local/zero-shot-experiments/zero-shot-subject-10/checkpoint_best_complex_kl.pt
  # local
  #seen_checkpoint: local/experiments/checkpoint_best_distmult.pt


