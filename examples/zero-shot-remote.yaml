dataset.name: zero-shot-head-10
job.type: zero_shot
zero_shot:
  #{fold_in, closed_form}
  type: fold_in
  fold_in.max_triple: 64
  fold_in.incremental: False
  fold_in.freeze: True
  obtain_seen_model: load
  # {all, incremental}
  eval_type: incremental
  # remote
  seen_checkpoint: /work/pbetz/zero-shot-experiments/head-10/search-jobs-on-train-valid/distmult-zero-shot-head-10-1vsAll-kl-ax/00012/checkpoint_best.pt
  # local
  #seen_checkpoint: local/experiments/checkpoint_best_distmult.pt

eval.split: test
entity_ranking.filter_splits: ["train", "valid", "aux", "test"]
