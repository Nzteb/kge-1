dataset.name: zero-shot-head-10
dataset.preload_data: False
job.type: zero_shot
zero_shot:
  only_eval: False
  #{fold_in, closed_form, similarity}
  type: closed_form
  closed_form.max_triple: 64
  obtain_seen_model: load
  seen_checkpoint: /home/patrick/Desktop/kge/local/zero-shot-experiments/head-10/distmult-seen-model/checkpoint_best.pt
  # {all, incremental}
  eval_type: incremental
  fold_in:
    num_epoch: -1
  fold_in.incremental: True

model: complex
valid.split: valid_seen
entity_ranking.filter_splits: [
  "train",
  "valid",
  "aux",
  "test"
]
eval.split: test

